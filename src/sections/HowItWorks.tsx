import { SectionHeading } from '../components/SectionHeading'
const steps = [['01', 'We listen', 'We get to know your business, your customers and your goals.'], ['02', 'We plan', 'A clear, tailored strategy that focuses on what will move the needle.'], ['03', 'We grow', 'You get more visibility, more trust and more customers. Simple.']]
export function HowItWorks() { return <section className="section" id="how-it-works"><div className="container"><SectionHeading eyebrow="How it works" title="Good marketing starts with a good conversation." description="No jargon. No one-size-fits-all packages. Just thoughtful marketing that feels like an extension of your team." /><div className="steps">{steps.map(([number, title, text]) => <div className="step" key={number}><span>{number}</span><h3>{title}</h3><p>{text}</p></div>)}</div></div></section> }
